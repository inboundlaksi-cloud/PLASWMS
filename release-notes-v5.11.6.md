# PLAS WMS v5.11.6 — แก้ยอด COPY AX มาไม่ครบ

วันที่: 7 สิงหาคม 2569

## สาเหตุที่แก้

AX สามารถแยก `Line No` เดียวกันเป็นหลาย Location และหลายจำนวนได้ ตัวอย่าง PO `P2621002955` มี 19 แถว แต่มี Line No ไม่ซ้ำเพียง 10 ค่า รุ่น v5.11.5 จึงเข้าใจว่าแถวที่เหลือเป็นข้อมูลซ้ำและนำเข้าเพียง 10 แถว

## การแก้ไข

- เปลี่ยนรหัสตรวจซ้ำเป็น Warehouse + PO + Line No + Item + Location + Batch + Quantity
- Line No เดียวกันแต่ Location หรือจำนวนต่างกัน จะถูกนำเข้าแยกเป็นคนละรายการ
- รองรับข้อมูลที่เคยนำเข้าด้วย v5.11.5: เมื่อวางชุดเดิมซ้ำ ระบบข้าม 10 แถวเดิมและเพิ่มเฉพาะ 9 แถวที่ขาด
- รองรับหัวตาราง AX ที่สลับตำแหน่งคอลัมน์ และข้อความ tab ที่มาในรูป `&#x9;`
- บันทึกข้อมูลด้วย Firestore batch เพื่อไม่แจ้งว่าสำเร็จเมื่อข้อมูลขึ้นไม่ครบ
- หากบันทึกไม่สำเร็จ ระบบเก็บหน้าพรีวิวไว้และแจ้งจำนวนที่บันทึกได้จริง

## ความเข้ากันได้

- กฎเดิมยังคงใช้: External item number เป็นรหัสหลัก, Item number เป็นรหัสรอง
- Quantity เป็นจำนวนรับเข้า
- Qty Onhand เป็นจำนวนคงเหลือ และใช้ InventQty เฉพาะเมื่อ Qty Onhand ว่าง
- รูปแบบ PLAS เดิม 8 คอลัมน์ยังใช้งานได้
