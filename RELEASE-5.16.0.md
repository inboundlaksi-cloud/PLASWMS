# PLAS WMS 5.16.0

ฉบับรวมปรับหน้าตาและการใช้งาน PC / มือถือ — 11 กันยายน 2026

## สิ่งที่รวมในรุ่นนี้

- หน้าเข้าสู่ระบบเลือกจากโปรไฟล์เดิม รักษารูปและขั้นตอน PIN เดิม
- ฉากคลังสินค้าและมาสคอตจิบิสามกิจกรรม: วิ่งขนของ สแกน/ส่งกล่อง และถือป้ายโหลด ใช้ภาพท่าทาง 24 เฟรมต่อกิจกรรม
- หยุดอนิเมชันเมื่อซ่อนหน้าเข้าสู่ระบบ/โหลด หรือซ่อนแท็บ และรองรับ reduced motion
- ธีมพาสเทล ไอคอน ช่องว่าง และองค์ประกอบสำหรับจอใหญ่และจอแคบ
- รหัส SAP และ COMP แยกสี มีปุ่ม Copy โดยไม่ต้องคลุมข้อความ
- ช่อง Location แนะนำตำแหน่งเดิมและประวัติที่เคยใช้ รองรับลูกศร Enter และ Escape
- หน้ากรอก Location สำหรับเครื่องสแกนแบบส่งข้อความหรือพิมพ์เอง พร้อมตรวจค่าว่าง ไม่บันทึกงานจนกดขั้นตอนยืนยันเดิม
- สถานะด่วนใช้สีธรรมชาติ ยกเลิกเอฟเฟกต์ RGB
- ตัวเลือกสติกเกอร์ TSC และใบคุมแยกพิมพ์ได้ หรือเลือกทั้งคู่ รักษารหัสจับคู่เดิม
- หน้าประวัติจัดกลุ่มตามวัน/คน ค้นหา Item / Location / ผู้ทำรายการ และแสดงเพิ่มเติมครั้งละ 60 รายการ
- หน้ารายการปัญหาและแบบฟอร์มแจ้งปัญหาแสดงบริบทสินค้า รักษาการแนบรูปและรายละเอียดเดิม
- จัดหน้าโปรไฟล์และตั้งค่าใหม่ รวมถึงสถานะไม่มีข้อมูลและช่องควบคุมบนมือถือ
- แก้ Copy PO ค้างค่าของสินค้าก่อนหน้า แก้ชื่อไฟล์ฟอนต์ไอคอนให้ตรงบนระบบที่แยกตัวพิมพ์ใหญ่–เล็ก
- ปรับเวอร์ชันแคชและรวมไฟล์หน้าจอใหม่ใน service worker

การเชื่อมต่อค้นหา On-hand จาก AX 2009 ยังไม่เพิ่ม ตามขอบเขตที่ตกลงไว้ ข้อมูลและวิธีเข้าสู่ระบบเดิมยังเป็นแหล่งข้อมูลจริง

## การนำไปใช้

แตก ZIP แล้วใช้โฟลเดอร์ที่มี index.html เป็นโฟลเดอร์เผยแพร่ของเว็บเดิม ไฟล์นี้รวมทั้งแอปและ assets ไม่ใช่เฉพาะไฟล์แก้ไข ไม่ต้องใช้โฟลเดอร์ dist เก่า

สำหรับ Netlify ที่ใช้งาน Functions ให้เผยแพร่ด้วยขั้นตอนเดิมของโครงการ เพื่อให้ netlify/plas-functions ถูก deploy ด้วย การลากไฟล์ static อย่างเดียวไม่ได้สร้าง Functions ใหม่

## ผลตรวจ

- Offline DOM regression ผ่าน 28 ข้อ รายละเอียดอยู่ใน TEST-RESULTS.json
- ตรวจ syntax ของ JavaScript ระดับ root 25 ไฟล์ ผ่าน
- parse CSS ระดับ root 17 ไฟล์ ผ่าน
- ตรวจไฟล์ที่หน้า HTML, CSS และ service worker อ้างถึง ผ่าน
- ชุดทดสอบปิดการเชื่อมต่อฐานข้อมูลและห้ามเขียนข้อมูลจริง

ข้อจำกัดของหลักฐาน: ไม่ได้ตรวจภาพด้วยเบราว์เซอร์จริงในสภาพแวดล้อมนี้ เนื่องจากไม่มี browser runtime และดาวน์โหลดไม่ได้ ไม่ได้ทดสอบบัญชี Firebase จริงหรือเครื่องพิมพ์ TSC จริง ดังนั้นผลข้างต้นไม่ใช่การรับรองภาพทุกขนาดจอหรือผลพิมพ์บนฮาร์ดแวร์

สำหรับผู้พัฒนา: warehouse-tests.cjs ใช้ Node.js และ jsdom รันด้วย npm run check:warehouse หลังเตรียม jsdom ในสภาพแวดล้อมทดสอบ
