# PLAS WMS v5.11.5 — คัดลอกข้อมูลจาก AX แล้ววาง

วันที่: 6 สิงหาคม 2569

## เพิ่มใหม่

- เพิ่มปุ่ม **วางข้อมูล AX** ในหน้า Admin
- รองรับการคัดลอกหัวตารางและหลายรายการจาก AX/Excel แล้ววางโดยตรง
- อ่านคอลัมน์จากชื่อหัวตาราง ไม่ยึดตำแหน่ง จึงข้ามคอลัมน์อื่นที่ติดมาจาก AX ได้
- รองรับหลาย Warehouse และหลาย Purchase order ในการวางครั้งเดียว
- แสดงหน้าตรวจสอบจำนวนที่พร้อมนำเข้า รายการซ้ำ และแถวข้อมูลไม่ครบก่อนยืนยัน

## การจับคู่ข้อมูล

- `External item number` → รหัสสินค้าหลัก
- `Item number` → รหัสสินค้ารอง
- `Purchase order` → เลขที่ PO
- `Text` → ชื่อสินค้า
- `Quantity` → จำนวนรับเข้า
- `Location` → Location เดิม
- `Batch number` → Batch
- `Qty Onhand` → ยอดคงเหลือ; ถ้าว่างใช้ `InventQty`

## ป้องกันข้อมูลซ้ำ

- ใช้ `Warehouse + Purchase order + Line No` เป็นรหัสอ้างอิง AX
- ข้ามรายการที่มีอยู่ในระบบแล้วโดยไม่เขียนทับ
- ข้ามแถวซ้ำภายในข้อมูลที่วางครั้งเดียวกัน
- ตรวจรายการซ้ำอีกครั้งก่อนบันทึกจริง

## ความเข้ากันได้

- รูปแบบ PLAS เดิม 8 คอลัมน์ยังใช้งานได้
- ไม่มีการเปลี่ยนขั้นตอนรับเข้า เขียนแล้ว พิมพ์ป้าย TOPUP, F-Zone, Academy League หรือ Supervisor
