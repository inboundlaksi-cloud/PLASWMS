# PLAS WMS 5.20.0 — Pastel Workspace

## ดีไซน์ที่เปลี่ยน

- Login เปลี่ยนเป็นฉากคลังภาพเดียวพร้อมตัวละครที่อยู่ในภาพจริง แผงโปรไฟล์ลอยทางขวาบน PC และเลื่อนลงใต้ฉากบนมือถือ ใช้โปรไฟล์ รูป และขั้นตอน PIN เดิม
- ภาพพื้นหลังเป็นภาพนิ่งตามแบบที่อนุมัติ ไม่มีมาสคอตจาก sprite ซ้อนบน Login
- หน้ารอโหลดใช้มาสคอตสแกนสินค้าเพียงตัวเดียว ข้อความอยู่นอกภาพ และซ่อนทั้งชุดเมื่อ showLoading(false)
- PC เปลี่ยนแท็บแนวนอนเป็นเมนูซ้าย มือถือเป็นเมนูล่าง รับเข้า / เติมสินค้า / ย้าย / เพิ่มเติม โดยยังเข้าถึงทุกแท็บเดิมได้
- หน้าเริ่มงานมีสรุปจำนวนจากข้อมูลจริง ใช้สีครีม เขียว sage ม่วง lavender และฟ้าอ่อน
- รายการสินค้าเรียงเป็นการ์ดกระชับ SAP สีน้ำเงิน COMP สีม่วง พร้อม Copy โดยไม่ใส่รูปสินค้าสมมติในข้อมูลจริง
- หน้ารายละเอียดเปลี่ยนหัวสีเข้มเป็นม่วงอ่อน แสดงจำนวนและ Location ชัด ช่อง Location ยังคงรายการแนะนำและการควบคุมคีย์บอร์ด
- ประวัติ ปัญหา โปรไฟล์ ตั้งค่า เติมสินค้า ย้าย และหน้าพิมพ์ใช้สีพื้น เส้นขอบ และองค์ประกอบร่วมกัน
- รักษาการแก้งานย้ายไม่ให้ปะปนหน้าเขียนแล้วและงานรับเข้า

## เผยแพร่

ZIP รวมโครงการเต็ม ใช้โฟลเดอร์ที่มี index.html เป็น root ไม่ต้องคัดเฉพาะ CSS และไม่ใช้ dist เก่า
สำหรับเว็บที่ใช้ Netlify Functions ต้องใช้ขั้นตอน deploy โครงการเดิมเพื่อเผยแพร่ netlify/plas-functions ด้วย
ยังไม่ได้เผยแพร่ขึ้นเว็บจริงให้โดยอัตโนมัติ

## ตรวจสอบ

- Offline DOM regression 35 ข้อ ผ่าน (TEST-RESULTS.json)
- JavaScript ระดับ root 26 ไฟล์ตรวจ syntax ผ่าน
- CSS ระดับ root 18 ไฟล์ parse ผ่าน และไฟล์ภาพ/ฟอนต์ที่อ้างถึงมีครบ
- เบราว์เซอร์จริงเปิดไม่ได้: ไม่มี Chromium executable ในสภาพแวดล้อมนี้ จึงยังไม่มีหลักฐานภาพหน้าจอจริง PC/มือถือ และยังไม่ได้ทดสอบฐานข้อมูลหรือเครื่องพิมพ์จริง
- การค้นหา On-hand จาก AX 2009 ไม่ได้เพิ่ม ตามที่ตกลงไว้

ไฟล์ดีไซน์หลัก: pastel-workspace.css, pastel-workspace.js, assets/login-warehouse-v520.png
