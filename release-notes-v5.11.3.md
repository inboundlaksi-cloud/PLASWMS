# PLAS WMS v5.11.3 — ภาษาไทยและอันดับพนักงาน

## คำเรียกเวลาที่เข้าใจง่าย

- เปลี่ยนข้อความ `SLA` ที่ผู้ใช้มองเห็นเป็น `เวลามาตรฐาน`
- งานที่ใช้เวลานานกว่าเกณฑ์แสดงเป็น `เกินเวลาที่กำหนด` หรือ `เกินเวลา`
- แก้ข้อความในภาพรวม คิวงาน งานค้าง ศูนย์งานผิดปกติ โปรไฟล์พนักงาน ส่งต่องาน รายงานพิมพ์ และ CSV
- ชื่อฟิลด์ภายในระบบยังเหมือนเดิม จึงไม่กระทบข้อมูลเดิมใน Firestore

## อันดับพนักงานที่ตรงกับคะแนนจริง

- เรียงอันดับด้วยคะแนนผลงานจากมากไปน้อย
- หากคะแนนผลงานเท่ากัน ใช้ EXP ช่วงนี้ คุณภาพงาน จำนวนงาน และแต้มรางวัลที่ได้ในช่วงนี้ตามลำดับ
- เพิ่มคำอธิบายเกณฑ์เรียงไว้เหนือรายชื่อพนักงาน
- เปลี่ยนหัวข้อ `Performance` เป็น `คะแนนผลงาน ↓`

## แก้คะแนนเริ่มต้นที่ทำให้อันดับผิด

- ก่อนหน้านี้พนักงานที่ไม่มีงานถูกตั้งคุณภาพเริ่มต้นเป็น 100% จึงได้คะแนนผลงาน 30 เท่ากันทุกคน
- เวอร์ชันนี้ผู้ยังไม่มีงานเริ่มที่คะแนนผลงาน 0 และคุณภาพ 0%
- ผู้ยังไม่มีผลงานแสดงเครื่องหมาย `—` แทนอันดับ และไม่ขึ้นแท่น Top 3
- ไม่กำหนดความถนัด `Location Writer` ให้คนที่ยังไม่มีงานอีกต่อไป

## การตรวจสอบ

- `npm run check`: ผ่าน 20 ชุดทดสอบ
- เพิ่มทดสอบลำดับ Performance → EXP → Quality
- เพิ่มทดสอบสถานะผู้ไม่มีงานและการไม่ขึ้น Top 3
- ตรวจ syntax และข้อความภาษาไทยในทุกจุดที่เกี่ยวข้อง
- Impeccable CLI ติดตั้งไม่ได้เนื่องจากนโยบายเครือข่ายของสภาพแวดล้อม จึงตรวจ UI และ responsive rules แบบ manual แทน
