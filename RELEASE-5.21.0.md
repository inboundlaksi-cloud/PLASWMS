# PLAS WMS 5.21.0 — การบันทึกและหน้าทำงาน

## สิ่งที่เปลี่ยน

- แถบสถานะบันทึก: กำลังบันทึก / บันทึกล่าสุดสำเร็จ / บันทึกไม่สำเร็จ อิงผลตอบกลับของวิธีบันทึกหลัก ไม่ใช้ตัวจับเวลาจำลองความสำเร็จ
- ยืนยัน Location และแจ้งปัญหาบันทึกรายการกับประวัติใน batch เดียวก่อนเปลี่ยนข้อมูลในหน้าจอ
- รับงานเติม จบงานเติม และยืนยันย้ายตรวจเอกสารบนเซิร์ฟเวอร์ผ่าน transaction ก่อนบันทึก ป้องกันงานที่เสร็จแล้วหรือถูกคนอื่นเปลี่ยนไปแล้ว
- ยืนยันย้ายบันทึกงาน ประวัติ และสินค้าปลายทางด้วยกัน ไม่สร้างปลายทางในเครื่องก่อนรู้ผลสำเร็จ
- ล็อกการกดยืนยันซ้ำระหว่างรอ และคืนปุ่มเมื่อคำขอจบ
- ปรับ FZONE hooks ให้รอผลบันทึก PLAS ก่อนซิงก์ placement และไม่แจ้งจบงานสำเร็จเมื่อ PLAS คืนค่าล้มเหลว
- แสดงสถานะข้อมูลสินค้า เติม ย้าย และประวัติ: แยกข้อมูลจาก cache, ข้อมูลจากเซิร์ฟเวอร์ และเชื่อมต่อไม่สำเร็จ เวลาในแถบรวมใช้เวลายืนยันที่เก่าที่สุดของทั้งสี่ชุด ดูรายละเอียดแต่ละชุดได้จาก tooltip ไม่อ้างว่า navigator.onLine ยืนยันการเชื่อมต่อฐานข้อมูลได้
- จำคำค้นหา chips โหมดเอา/ไม่เอา สถานะรับเข้า หมวดประวัติ รูปแบบประวัติ และหมวด monitor แยกตามผู้ใช้บนเบราว์เซอร์นี้ มีปุ่มคืนตัวกรองเริ่มต้น ไม่เก็บ PIN ในข้อมูลตัวกรอง
- การ์ด ITEM ยาวแบ่งพื้นที่ Copy ชัด จำนวนและ Location จัดแนวเดียวกัน
- กลุ่มงานเติมใช้สีฟ้า / ม่วง / เขียว แยกงานย้ายเป็นกลุ่มตามสถานะจริง โดยไม่เพิ่มสถานะสมมติให้ข้อมูลเก่า
- ประวัติแสดงและค้นหาหมายเหตุ สาเหตุ และผลดำเนินการที่มีอยู่ หน้าปัญหาแสดงรายละเอียด ผู้แจ้ง และวันที่ตามข้อมูลจริง
- หน้ามือถือปรับพื้นที่รายละเอียดตาม visualViewport ขณะแป้นพิมพ์เปิด และจำกัดความสูงรายการ Location แนะนำ
- รักษาเมนูที่ระบบสิทธิ์ซ่อนไว้ ไม่ให้ CSS ของเมนูมือถือแสดงกลับขึ้นมา
- คงฉาก Login แบบไม่มีคำคม สีแอดมินอ่อน และการแยกงานย้ายออกจากเขียนแล้ว

## ผลทดสอบ

Offline regression 49 ข้อผ่าน รวมรอบบันทึกสำเร็จ ล้มเหลว รอผล กดซ้ำ สถานะงานเปลี่ยน จำตัวกรองแยกคน ข้อมูล cache / server / offline และข้อความประวัติที่มี HTML

ตรวจ JavaScript ระดับ root 27 ไฟล์ และ parse CSS ระดับ root 19 ไฟล์ผ่าน ดูรายชื่อข้อทดสอบใน TEST-RESULTS.json

## ขอบเขตหลักฐาน

ทดสอบด้วยฐานข้อมูลจำลองในหน่วยความจำ ไม่มีการเขียนฐานข้อมูลบริษัท ไม่ได้ทดสอบ transaction กับกฎ Firestore ของบัญชีจริง และไม่ได้ทดสอบเครื่องพิมพ์ TSC จริง
ไม่มี browser executable ในสภาพแวดล้อมนี้ จึงยังไม่ได้ตรวจภาพหรือแป้นพิมพ์มือถือบนเบราว์เซอร์จริง

สถานะบันทึกครอบคลุม saveToFirebase, saveManyToFirebase และ commitWorkflowDocuments ส่วนบริการภายนอกที่บันทึกผ่าน API ของตนเองยังใช้การรายงานผลของบริการเดิม ไม่ได้เปลี่ยนทุก integration ให้เป็น transaction ข้ามระบบ

## นำไปใช้

ZIP รวมโครงการเต็ม ใช้โฟลเดอร์ที่มี index.html เป็น root ของ deploy เดิม รวม Netlify Functions ตามขั้นตอนเดิม ไม่ใช้ dist เก่า
ยังไม่ได้เผยแพร่ขึ้นเว็บไซต์จริง ไม่เปลี่ยน Firestore Rules หรือข้อมูลจริง และยังไม่เพิ่มการค้นหา On-hand จาก AX 2009
