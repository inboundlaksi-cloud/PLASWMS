# PLAS WMS v5.13.4 — ปิด QuickHub แล้วแตะพื้นที่ว่างไม่ได้

## ต้นเหตุ

กฎ `.qh-items` ใน `style.css` โหลดหลัง Tailwind และทับ `.hidden` ทำให้เมนู QuickHub โปร่งใส แต่ปุ่มย่อยยังคงอยู่บนหน้าจอและรับการแตะได้

## สิ่งที่แก้

- เมื่อปิด QuickHub เมนูและฉากหลังถูก `display:none` และ `pointer-events:none` อย่างเด็ดขาด
- ปุ่ม QuickHub ปิดการรับสัมผัสเป็นค่าเริ่มต้น และเปิดรับสัมผัสเฉพาะตอนเมนูแสดงจริง
- เพิ่ม `inert` และ `aria-hidden` เป็นชั้นป้องกันสำหรับมือถือและ WebView
- ปุ่มหลักแจ้งสถานะเปิด–ปิดด้วย `aria-expanded`
- บังคับโหลด `style.css`, `script.js` และ Service Worker รุ่น v5.13.4 เพื่อไม่ใช้ไฟล์เก่า
- ยังคงระบบป้องกันเด้งไปหน้าพิมพ์ป้ายหลังยืนยันงานจาก v5.13.3

## การ Deploy

Deploy ZIP นี้ทับเว็บไซต์ PLAS WMS เดิมบน Netlify ได้ทันที ไม่ต้องเพิ่มหรือแก้ Secret

หลัง Deploy ให้ปิดแท็บ PLAS WMS และ QuickHub บนมือถือทั้งหมดแล้วเปิดใหม่ หากมีแถบ “มีเวอร์ชันใหม่” ให้กด “อัปเดต”
