# PLAS WMS v5.13.6 — Top Location & Header Layout Safe

## สิ่งที่แก้ไข

- เปิดแท็บ **Top Up (เติมของ) / Top Location** จะไม่เด้งถาม PIN ของ F‑Zone อีกต่อไป
- การตรวจพาเลท F‑Zone แบบเบื้องหลังเป็นโหมดเงียบ หากยังไม่มี Session จะแสดง **แตะเมื่อต้องการตรวจพาเลท** แทน
- PIN จะแสดงเฉพาะเมื่อผู้ใช้กดเปิดข้อมูลหรือแผนผัง F‑Zone ด้วยตนเอง
- แถบสีด้านบนที่บอกบทบาท เช่น **ทีมพนักงาน** มีพื้นที่ของตัวเองและไม่ทับหัวแอป
- แถบสรุปงานและข้อความแจ้งเตือนเรียงต่อจากแถบบทบาท ไม่ซ้อนกัน
- ตัด tooltip ที่ลอยทับแท็บงานหลักออก แต่ยังเก็บคำอธิบายสำหรับโปรแกรมอ่านหน้าจอ
- ปรับตำแหน่ง tooltip ของปุ่มไอคอนที่ยังจำเป็นให้อยู่ในขอบจอ
- ขยายตัวอักษรปุ่มกรอง/ปุ่มการทำงานในแถบรับเข้าให้อ่านง่ายขึ้น
- ปรับรายละเอียดการ์ดและ modal ที่เครื่องมือตรวจ UI พบว่าใช้เส้นเน้นหรือ animation ที่ทำให้หน้าดูไม่เรียบร้อย

## พฤติกรรมที่ตั้งใจไว้

1. กด **Top Up (เติมของ)**: เปิดรายการงานทันที ไม่มีหน้าต่าง PIN
2. หาก F‑Zone เชื่อมแล้ว: การ์ดแสดงข้อมูลพาเลทได้ตามปกติ
3. หาก F‑Zone ยังไม่เชื่อม: การ์ดแสดงปุ่มตัวเลือก F‑Zone โดยไม่ขัดจังหวะงาน
4. กดปุ่ม F‑Zone โดยตั้งใจ: ระบบจึงขอ PIN และเปิดข้อมูล F‑Zone

## หลัง Deploy

- รอ Netlify Deploy สถานะ Published
- เปิดเว็บแล้วกดรีเฟรชแบบล้างแคชหนึ่งครั้ง (`Ctrl + Shift + R`)
- หากติดตั้งเป็น PWA ให้ปิดแอปแล้วเปิดใหม่ เพื่อรับ Service Worker `plas-wms-v5.13.6-toplocation-layout-safe`

