# 5.16.1 — แก้ตามภาพหน้าจอ

- ย้ายการ blend ภาพไปยังชั้นของกลุ่มตัวละคร เพื่อให้พื้นสีขาวผสมกับฉากจริง ไม่ถูกแยกไว้ใน stacking context ของแต่ละกลุ่ม
- หน้าล็อกอินเหลือมาสคอตทำงาน ไม่มีตัวถือป้ายโหลดค้างอยู่ และจัดตำแหน่งตัวละครที่ระดับพื้น
- showLoading(false) ซ่อนทั้ง overlay ด้วย hidden attribute และ class พร้อมกฎ CSS ชัดเจน
- งานปลายทางที่มี sourceMoveId ไม่เข้าหน้าเขียนแล้วหรือรายการรับเข้ารวม ไม่ลบข้อมูล ไม่แก้จำนวนหรือประวัติงานย้าย
- การ์ดเรียงจากด้านบน ไม่ยืดเต็มความสูงแถว ย้ายป้ายสถานะออกจากแนวเลข ITEM ลดปัญหาเลขถูกเบียด
- ทดสอบ offline DOM 31 ข้อ ผลอยู่ใน TEST-RESULTS.json

ไม่ได้ตรวจภาพด้วยเบราว์เซอร์จริงในสภาพแวดล้อมนี้ จึงยังไม่ยืนยันความเรียบร้อยของภาพ blend ทุกอุปกรณ์ การแก้ครั้งนี้เป็น CSS composition ไม่ใช่การเปลี่ยนไฟล์ภาพให้เป็นพื้นโปร่งใสจริง
