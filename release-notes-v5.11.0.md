# PLAS WMS v5.11.0 — Label Pairing Scanner

## สิ่งที่เปลี่ยน

- สร้างเลขจับคู่ถาวรให้ทุก item record รูปแบบ `PL-XXXXXXXX` แม้เลข ITEM ซ้ำกันก็ได้เลขคนละดวง
- เปลี่ยนขั้นตอนเป็น `ยังไม่พิมพ์` → `พิมพ์แล้ว · รอติด` → `ติดแล้ว · เขียนแล้ว`
- การพิมพ์ไม่เปลี่ยนเป็น Written อัตโนมัติอีกต่อไป
- เพิ่ม Scanner ภายใน PLAS พร้อมตะกร้าตรวจสอบก่อนยืนยันจริง
- เพิ่มช่องกรอกเลขจับคู่/ID/URL สำหรับเครื่องที่ไม่รองรับ BarcodeDetector หรือไม่อนุญาตกล้อง
- เพิ่มทางเลือก `เขียนมือ · ไม่ใช้ป้าย` และเครื่องมือจัดประเภทข้อมูลเก่า
- การพิมพ์ซ้ำต้องระบุเหตุผล และป้ายใหม่กลับไปอยู่สถานะรอติด
- เพิ่มเลขจับคู่บนป้าย NIIMBOT, TSC และ A4 โดย QR ภายนอกยังคงเป็นหน้าตรวจสอบข้อมูลแบบเดิม

## ความปลอดภัยของข้อมูลเดิม

สถานะ `confirmed/reprinted` จากระบบ auto-confirm รุ่นเดิมจะถูกแสดงเป็น `ต้องตรวจสอบข้อมูลเก่า` ไม่ถือว่าติดป้ายจริง ผู้ใช้เลือกได้ว่า ต้องพิมพ์ป้าย, เขียนมือ หรือยืนยันว่าติดแล้ว ประวัติรอบพิมพ์และ audit เดิมจะถูกย้ายมาเก็บต่อในโครงสร้าง v3

## การตรวจสอบ

- `npm run check`: ผ่าน 20 ชุดทดสอบ
- Impeccable detector: ไม่พบข้อเตือนในโมดูลที่แก้
- Visual QA: desktop 1440×900 และ mobile 390×844 ไม่มี horizontal overflow หรือ console error
- ตรวจ touch target หลักบนมือถืออย่างน้อย 44px และตัวอักษรในโมดูลไม่น้อยกว่า 12px
