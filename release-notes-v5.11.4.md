# PLAS WMS v5.11.4 — แก้เปิด F-Zone ไม่ได้เมื่อโควตาเต็ม

## อาการเดิม

เมื่อบริการ `fzone-session` หรือ `fzone-bridge-issue` ชนข้อผิดพลาด `RESOURCE_EXHAUSTED: Quota exceeded` ระบบจะเปิดแท็บ `about:blank` แล้วแสดงเพียงหน้าข้อผิดพลาด ทำให้เข้า BORNEO F-Zone ไม่ได้

## การทำงานใหม่

- ถ้า Bridge ทำงานปกติ ระบบยังใช้ One-time Ticket และเข้าสู่ F-Zone ด้วยบัญชี PLAS เหมือนเดิม
- ถ้า Bridge ใช้โควตาครบชั่วคราว ระบบจะเปิด Portal F-Zone เดิมโดยตรงแทนหน้าข้อผิดพลาด
- หากเบราว์เซอร์มี Session F-Zone อยู่แล้ว จะเข้าแอปต่อได้ทันที หากไม่มีจะพบหน้า Login ของ F-Zone
- พารามิเตอร์ `tab`, แผนผัง, Location, Point และ Pallet ที่ผู้ใช้ต้องการเปิดจะติดไปกับทางสำรอง
- ไม่มีการส่ง PIN ผ่าน URL และไม่มีการสร้าง Ticket ปลอม

## ลดการใช้โควตาซ้ำ

- หลังสร้าง PLAS/F-Zone Session ไม่สำเร็จ ระบบพักการลองใหม่ 30 วินาที
- การกดเปิด F-Zone ระหว่างช่วงพักจะใช้ผลล้มเหลวเดิมและเปิดทางสำรองทันที
- ป้องกันกรณี Login hook ล้มเหลวหนึ่งครั้งแล้วปุ่มเปิด F-Zone ยิงคำขอเดิมซ้ำอีกครั้ง

## ฝั่งเซิร์ฟเวอร์

- ตรวจจับ Firestore code 8, `resource-exhausted` และ `Quota exceeded`
- ตอบเป็น HTTP 503 พร้อม code `BRIDGE_QUOTA_EXHAUSTED`
- คงการตรวจสิทธิ์ PLAS ซ้ำก่อนออก One-time Ticket เมื่อบริการพร้อมใช้งาน

## การติดตั้ง

อัปเดต PLAS WMS ทั้งชุดเพื่อให้ไฟล์หน้าเว็บและ Netlify Functions เป็นเวอร์ชันเดียวกัน ไม่ต้องอัปเดตตัว BORNEO F-Zone สำหรับการแก้นี้
